/**
 * Sync catalog names into previews + registry.
 * Delegates to extend-invoice-formats-72-85.mjs (single source of name truth).
 * Usage: node scripts/sync-invoice-catalog-names.mjs
 */
import './extend-invoice-formats-72-85.mjs';
