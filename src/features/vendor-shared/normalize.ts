export {
  asRecord,
  filenameFromContentDisposition,
  normalizeMeta,
  pickNumber,
  pickString,
  safeDownloadFilename,
  unwrapData,
  unwrapList,
} from '@/features/portal-shared/normalize';
export type { PortalPaginationMeta as VendorPaginationMeta } from '@/features/portal-shared/normalize';
