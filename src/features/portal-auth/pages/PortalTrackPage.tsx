import { Card } from '@/components/ui/Card';

export default function PortalTrackPage() {
  return (
    <Card className="p-6">
      <h1 className="text-lg font-semibold text-[var(--color-neutral-800)]">Track</h1>
      <p className="mt-2 text-sm text-[var(--color-neutral-400)]">
        Public Track &amp; Trace + portal track widget will be implemented in a later phase.
      </p>
    </Card>
  );
}

