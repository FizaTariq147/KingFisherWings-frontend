import { Card } from '@/components/ui/Card';

export default function PortalQuotesPage() {
  return (
    <Card className="p-6">
      <h1 className="text-lg font-semibold text-[var(--color-neutral-800)]">Quotes</h1>
      <p className="mt-2 text-sm text-[var(--color-neutral-400)]">
        Coming soon — quotation list/detail will be added in a later portal phase.
      </p>
    </Card>
  );
}

