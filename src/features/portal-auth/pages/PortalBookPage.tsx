import { Card } from '@/components/ui/Card';

export default function PortalBookPage() {
  return (
    <Card className="p-6">
      <h1 className="text-lg font-semibold text-[var(--color-neutral-800)]">Book</h1>
      <p className="mt-2 text-sm text-[var(--color-neutral-400)]">
        Coming soon — book shipment flow will be added in a later phase.
      </p>
    </Card>
  );
}

