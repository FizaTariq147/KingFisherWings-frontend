import { Navigate } from 'react-router-dom';

/** Export-Air documentation tile → shared air export jobs hub. */
export default function ExportAirPage() {
  return <Navigate to="/jobs/air-export" replace />;
}
